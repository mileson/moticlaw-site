<!-- OPENPRD:GENERATED
adapter=codex
source=command:next
version=0.1.0
checksum=b2f09eb8cd2ffae7
-->

# OpenPrd Next

Run `openprd status .` and `openprd next .`, summarize the current gate, then execute the suggested OpenPrd action when safe.

Always rebuild state from `.openprd/` before acting.
